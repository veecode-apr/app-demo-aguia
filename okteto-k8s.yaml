apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-demo-aguia 
spec:
  selector:
    matchLabels:
      app: app-demo-aguia
  template:
    metadata:
      labels:
        app: app-demo-aguia
    spec:
      containers:
        - image: okteto.dev/java-gradle-app-demo-aguia:1.0.0
          name: app-demo-aguia

---

apiVersion: v1
kind: Service
metadata:
  name: app-demo-aguia
spec:
  type: ClusterIP
  ports:
    - name: "app-demo-aguia"
      port: 8080
  selector:
    app: app-demo-aguia

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-demo-aguia
  annotations:
    dev.okteto.com/generate-host: app-demo-aguia
spec:
  rules:
    - http:
        paths:
          - backend:
              service:
                name: app-demo-aguia
                port:
                  number: 8080
            path: /
            pathType: ImplementationSpecific