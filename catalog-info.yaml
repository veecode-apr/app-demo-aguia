apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: app-demo-aguia-location
  description: Location to import all app-demo-aguia catalog components
spec:
  targets:
    - ./.platform/*.yaml
